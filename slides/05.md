#  5/6  Features coming soon

- support for missing lifecycle methods
  - `getSnapshotBeforeUpdate()`
  - `componentDidCatch()`
  - "soon" (according to React docs)

- *Redux* hooks
  - `useActions()`, `useSelectors()` etc.
  - currently in alpha, should be out very soon
